# Beat-537 attempt: keep the 16 extras, restore `sub_308` ranking — WON

Live: `bNsDafsS` **0.925724375** (AUC **0.95947223**, F1 0.903225806).

Upload first: `submissions/sub_542_f1lock537_restore308.csv`  
SHA-256 `bf7f5a8e71b78196321ff5e1096a6f42cc9628c85348c4df198709db6475f20c`  
15/15. 587 positives. **0 flips vs `sub_537` F1.** Pearson vs `308` **0.998**.

`sub_537` (`2F9tRW8b`, **0.923267266**) won F1 by adding 16 S1-only/4-month
`533` extras (+4 TP +2 FP → 168/23). `max()` also rewrote **532 of 571**
`308` positives (mean lift +0.147) and lost **107** public AUC pairs
(0.95722 → 0.95333).

This file:

- keeps every `537` decision (F1 locked at 0.903225806 if the public extras
  are the same 6 rows)
- puts raw `p308` back on the other 1,014 rows
- uses `p533` only on the 16 extras (already ≥ 0.5)

Measured: F1 held exactly. AUC **beat** `308` (0.95722 → 0.95947, +62
pairs) and `537` (+169 pairs). Blended **0.925724375**.

Hedge (do not upload in the same minute):
`submissions/sub_543_f1lock537_ranklock308.csv` — same 587 labels, full
within-side remap of `308` order. Spearman vs `308` 0.99995.

Select private **`bNsDafsS` + `Q7xLfCHt`** now. Do not upload `543` unless
you still have a spare slot and want a remap hedge.
